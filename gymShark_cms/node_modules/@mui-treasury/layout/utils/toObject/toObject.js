"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.toObject = toObject;

function toObject(array) {
  var secondParam = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : undefined;
  var thirdParam = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : undefined;
  var result = {};

  if (!secondParam && !thirdParam) {
    array.forEach(function (item) {
      result[item] = item;
    });
    return result;
  }

  if (typeof secondParam === 'function') {
    array.forEach(function (item) {
      result[item] = secondParam(item);
    });
    return result;
  }

  if (typeof secondParam === 'string' && !thirdParam) {
    array.forEach(function (item) {
      result[item[secondParam]] = item;
    });
    return result;
  }

  array.forEach(function (item) {
    result[item[secondParam]] = thirdParam(item);
  });
  return result;
}