"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = exports.switchDirection = void 0;

var _utils = require("../utils");

var switchDirection = function switchDirection(word) {
  if (typeof word !== 'string') return word;
  if (word.includes('Left')) return word.replace('Left', 'Right');
  if (word.includes('left')) return word.replace('left', 'right');
  if (word.includes('Right')) return word.replace('Right', 'Left');
  if (word.includes('right')) return word.replace('right', 'left');
  return word;
};

exports.switchDirection = switchDirection;

var _default = function _default(direction, style) {
  if (direction !== 'rtl') return style;
  return (0, _utils.mapObject)(style, function (_, key) {
    return switchDirection(key);
  });
};

exports["default"] = _default;