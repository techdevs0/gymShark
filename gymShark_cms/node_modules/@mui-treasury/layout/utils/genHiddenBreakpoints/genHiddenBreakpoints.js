"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.genHiddenBreakpoints = void 0;

var _createBreakpoints = require("@material-ui/core/styles/createBreakpoints");

var _get = _interopRequireDefault(require("../get"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

var genHiddenBreakpoints = function genHiddenBreakpoints(componentData, id, predicate) {
  if ((0, _get["default"])(componentData, ['hiddenById', id]) === _createBreakpoints.keys) {
    // hidden at all breakpoints if true
    return _createBreakpoints.keys;
  }

  var result = [];
  var found = false;

  _createBreakpoints.keys.forEach(function (bp) {
    var config = (0, _get["default"])(componentData, ['configMapById', id, bp]);

    if (predicate(config)) {
      // if predicate return true, won't add this breakpoint to the list
      found = true;
    } else if (!found || config) {
      result.push(bp);
      found = false;
    }
  });

  return result;
};

exports.genHiddenBreakpoints = genHiddenBreakpoints;