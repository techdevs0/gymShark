import { Dictionary } from '../../types';
import { RpsConfig } from '../../shared/State';
export declare const mapRpsById: <T, K = T>(rpsById: Dictionary<import("../../types").MapBreakpoint<T>>, iteratee: (key: string) => K) => {
    [x: string]: {
        xs?: T & K;
        sm?: T & K;
        md?: T & K;
        lg?: T & K;
        xl?: T & K;
    };
};
