import { Dictionary } from '../../types';
export declare const mapObject: <T, V extends T>(obj: Dictionary<T>, keyIteratee: (value: T, key: string) => string, valueIteratee?: (value: T, key: string) => V) => {
    [key: string]: V;
};
