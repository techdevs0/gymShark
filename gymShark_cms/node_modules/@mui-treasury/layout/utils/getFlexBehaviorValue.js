"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = exports.attachOperator = void 0;

var attachOperator = function attachOperator(value, operator) {
  if (typeof value === "number") {
    return operator * value;
  }

  if (typeof value === "string") {
    if (operator === 1) {
      return value;
    }

    if (operator === -1) {
      return "-".concat(value);
    }
  }
};

exports.attachOperator = attachOperator;

var getFlexBehaviorValue = function getFlexBehaviorValue(anchor, currentWidth) {
  if (anchor === "left") return attachOperator(currentWidth, 1);
  if (anchor === "right") return attachOperator(currentWidth, -1);
  return 0;
};

var _default = getFlexBehaviorValue;
exports["default"] = _default;