import { Breakpoint } from '@material-ui/core/styles/createBreakpoints';
import { HeaderConfig } from '../types';
import { SingleObjData } from '../shared/State';
export interface IHeaderRegistry {
    registerConfig: (breakpoint: Breakpoint, config: HeaderConfig) => IHeaderRegistry;
}
declare const HeaderRegistry: (state: SingleObjData<HeaderConfig>) => IHeaderRegistry;
export default HeaderRegistry;
