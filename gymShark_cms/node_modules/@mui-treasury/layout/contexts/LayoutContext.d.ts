import React from 'react';
import { ILayoutBuilder } from '../builders';
import { LayoutData, State } from '../types';
declare const Context: React.Context<ContextValue>;
export declare type ContextValue = {
    state: State;
    data: LayoutData;
    setOpen: (id: string, value: boolean) => void;
    setCollapsed: (id: string, value: boolean) => void;
};
export declare const useLayoutCtx: () => ContextValue;
export declare const LayoutConsumer: React.Consumer<ContextValue>;
export declare type LayoutProviderProps = {
    initialState?: State;
    scheme: ILayoutBuilder;
};
export declare const LayoutProvider: ({ initialState: controlledInitialState, scheme, children, }: React.PropsWithChildren<LayoutProviderProps>) => JSX.Element;
export default Context;
