import React from "react";
import { Theme } from "@material-ui/core/styles";
import { ContextValue, LayoutProviderProps } from "../../contexts";
declare type RootProps = {
    theme?: Theme;
    themeProviderOmitted?: boolean;
    children: FunctionChildren;
} & LayoutProviderProps;
declare type FunctionChildren = React.ReactNode | ((ctx: ContextValue) => React.ReactNode);
declare const Root: ({ theme, themeProviderOmitted, children, ...props }: React.PropsWithChildren<RootProps>) => JSX.Element;
export default Root;
