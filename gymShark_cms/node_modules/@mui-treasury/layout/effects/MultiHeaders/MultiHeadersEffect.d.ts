import { HeaderConfig } from '../../types';
declare const _default: (configs: HeaderConfig[]) => {
    getSidebarInteraction: (sidebarId: string) => {
        isClipped: boolean;
        clippedHeight: string | number;
        relativeHeight: string | number;
        nonRelativeHeight: string | number;
        maxHeightFromTop: string | number;
        nonRelativeOffset: string;
        maxOffset: string;
    };
};
export default _default;
