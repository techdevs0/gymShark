import { HeaderConfig } from '../../types';
declare const _default: (configs: HeaderConfig[]) => {
    relativeConfigs: HeaderConfig[];
    nonRelativeConfigs: HeaderConfig[];
    totalHeight: string | number;
    maxHeightFromTop: string | number;
};
export default _default;
