import { Theme } from "@material-ui/core/styles";
declare const _default: ({ transitions }: Theme) => {
    root: {
        transition: string;
        transitionProperty: string;
    };
    smooth: {
        transition: string;
    };
};
export default _default;
