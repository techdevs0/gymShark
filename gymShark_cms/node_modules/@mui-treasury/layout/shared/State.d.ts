import { Breakpoint } from '@material-ui/core/styles/createBreakpoints';
import { Dictionary, MapBreakpoint } from '../types';
export declare type RpsConfig<T> = MapBreakpoint<T>;
export declare type RpsConfigArray<T> = MapBreakpoint<T[]>;
export declare type SingleObjData<T, P = {}> = {
    id: string;
    props?: P;
    rpsConfig?: RpsConfig<T>;
    hidden?: Breakpoint[];
};
export declare type MultiObjData<T, P = {}> = {
    ids: string[];
    dataById: Dictionary<SingleObjData<T, P>>;
};
export declare const createSingleObjData: <T, P = {}>({ id, props, rpsConfig, hidden, }: SingleObjData<T, P>) => {
    id: string;
    props: P;
    rpsConfig: MapBreakpoint<T>;
    hidden: ("xs" | "sm" | "md" | "lg" | "xl")[];
};
export declare const createMultiObjData: <T, P = {}>(rpsConfigById?: Dictionary<MapBreakpoint<T>>) => MultiObjData<T, P>;
